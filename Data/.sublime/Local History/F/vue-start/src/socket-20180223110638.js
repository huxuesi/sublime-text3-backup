import store from './vueX';

export default function connectSocket(uid) {
    // document.cookie = `uid=${uid};path=/;domain=.`;
    // window.socket = io( imgUrl + ':3000' );
    // window.socket.on('notification', function(data){
    //     console.log('通知', data);
    // });
}
